Custom String Class

This progect implements a custom dynamic-string class c++.

Features
* Dynamic memory allocation
* Copy and move semantic
* String concatenation
* Support c-style string

Example in main.cpp file takes strings as arguments, converts them to lower case, 
and returns them sorted in reverse lexicographical order.